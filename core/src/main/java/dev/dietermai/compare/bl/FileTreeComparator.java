package dev.dietermai.compare.bl;

import dev.dietermai.compare.model.compare.ComparedTree;
import dev.dietermai.compare.model.file.FileTreeRoot;

public class FileTreeComparator {

	public ComparedTree compare(FileTreeRoot roota, FileTreeRoot rootb) {
		return new ComparedTree();
	}
	
}
